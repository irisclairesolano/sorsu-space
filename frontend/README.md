# SORSU Space Web (Next.js)

## Setup

```bash
npm install
npm run dev
```

## Notes

- Uses App Router + shadcn/ui + TanStack Query.
- Auth UI gates unverified accounts and requires `@sorsu.edu.ph` emails.
